{
  "scripts": {
    "start:app": "cd app && npm run dev",
    "build:app": "cd app && npm run build",
    "copy:dist": "cpx \"app/dist/**/*\" ./",
    "prepare:commit": "npm run build:app && npm run copy:dist && git add .",
    "prepare": "husky install"
  },
  "devDependencies": {
    "cpx": "^1.5.0",
    "husky": "^9.1.7"
  }
}
